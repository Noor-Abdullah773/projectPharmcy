import 'package:flutter/material.dart';

class Donits extends StatelessWidget {
  const Donits({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Text('Donat'),
      ),
    );
  }
}
